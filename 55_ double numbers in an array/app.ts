//make an array of numbers
let gainNumbers : number[] = [22, 40, 42, 50];
//double each number
let doubleNumbers = gainNumbers.map(gainNumbers => gainNumbers*2);
//new doubled array
console.log(doubleNumbers);